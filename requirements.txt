# Core
streamlit>=1.35.0
yfinance>=0.2.40
plotly>=5.20.0
pandas>=2.1.0
numpy>=1.26.0
pyyaml>=6.0.1
scipy>=1.12.0
scikit-learn>=1.4.0
requests>=2.31.0

# Portfolio optimisation (Riskfolio kept as verification fallback)
riskfolio-lib>=7.0.0

# Advanced charts
pandas-ta>=0.4.71b0

# AI + LLM
groq>=0.9.0

# Calendar export
icalendar>=5.0.12

# SEC EDGAR
edgartools>=5.0

# Scheduler
APScheduler>=3.10.0

# Julia bridge (optional — install Julia separately, then uncomment)
# juliacall>=0.9.0

# Hugging Face Spaces (Phase 3 host — no extra deps needed)
